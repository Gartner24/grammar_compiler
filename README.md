# BugScript Compiler to python

Este proyecto implementa un compilador para el lenguaje BugScript.

Para ejecutar el compilador de un archivo con extension .bs a .py

```bash
py main.py [archivo].bs
```
